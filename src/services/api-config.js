export const apiConfig = {
  baseUrl: "http://localhost:3333"
}



// por que se um dia a ip da api muda voce so precisa mudar aqui
// sempre que eu quiser fazer uma requisicao //exemplo referencia `${apiConfig.baseUrl}/schedule`